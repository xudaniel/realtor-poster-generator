# Synthetic MLS contract fixtures

Every name, identifier, address, image byte, and provider in this directory is fabricated for automated tests. The files contain no real listing record, credential, board feed, or permission grant. They may be used only to verify the normalized v1.4.1 connector contract and failure handling.
