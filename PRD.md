# 房地产海报生成器产品需求文档

[English PRD](PRD.en.md) · [中文 README](README.md) · [English README](README.en.md)

## 1. 文档信息

- 产品名称：房地产海报生成器
- 当前稳定版本：1.4.0
- 当前发布范围：稳定版故事 1–10 与横向恢复 Issue #20；Issue #22 的 v2 授权 MLS 号码候选功能
- 文档语言：中文
- 产品形态：浏览器本地手工编辑器、明确选择的授权 MLS 连接器、本地 Python 命令行工具与离线 HTML 预览工具
- 主要输出：完整海报 PNG/PDF、社交媒体 PNG/ZIP、可移植项目 JSON/YAML、模板与合规配置、审批包、校验清单 JSON、批次摘要 JSON、焦点预览 HTML

## 2. 产品背景

房地产经纪人在制作租赁或销售海报时，通常需要重复整理地址、价格、MLS、房屋数据、特色、设施、照片、户型图和联系方式。使用普通设计软件容易产生以下问题：

- 每套房源都要重新排版，耗时较长
- 地址、电话号码、价格或 MLS 可能被手动输错
- 照片比例不一致，容易被拉伸或错误裁切
- 文字过长时可能溢出、遮挡或变得难以阅读
- 不同经纪人制作的海报缺乏统一品牌风格
- 输出文件缺少可追溯的输入和资源校验信息

本产品通过结构化输入、确定性文字渲染、自动排版和统一品牌配置，将房源资料快速转换为专业的竖版海报及适合社交平台的自适应图片。1.3 将浏览器流程扩展为完整本地广告工作台；1.4.0 正式版加入参考样图启发的十个原创结构化模块，横向 Issue #20 则保证“生成—审核—纠错”流程可以恢复。

## 3. 产品目标

### 3.1 核心目标

1. 经纪人只填写一次结构化数据，即可生成完整海报。
2. 所有关键文字必须严格来自输入文件，不允许模型自动改写或猜测。
3. 常见字段错误和资源缺失必须在渲染前被发现。
4. 不同尺寸的照片必须自动旋转、缩放和裁切，不得变形。
5. 海报应具有稳定、专业、易读且可配置的品牌视觉效果。
6. 同一输入、图片、字体和软件版本应当产生可重复的视觉结果。
7. 输出应同时满足线上分享、打印和归档需要。
8. 同一批房源应能够在一次命令中完成预验证和统一输出。
9. 用户应能在不上传照片的情况下选择主图焦点。
10. 版式变更应能够通过自动化指标发现和审查。
11. 用户应能通过无需安装的浏览器流程实时预览并导出完整广告套件。
12. 中文与英文用户文档、产品需求文档应提供同等的已实现功能范围。
13. 浏览器导出必须经过配置化合规预检，并提供可追溯的本地审批记录。
14. 品牌模板与中英文内容必须可复用、可锁定并能跨项目移植。
15. 获授权用户应能通过一个精确 MLS 号码生成完整可编辑项目，并保留字段来源与图片许可记录。

### 3.2 非目标

- 不抓取房源网页、不绕过 MLS/数据商授权，也不在静态应用中保存数据商凭据
- 不自动发布到社交媒体或房地产平台
- 不判断广告内容是否合法或事实是否准确
- 不生成或修改真实房源照片
- 不替代专业设计师处理高度定制的广告活动
- 不提供多人在线协作或云端账户系统

## 4. 目标用户

### 4.1 主要用户

- 房地产销售代表
- 房地产经纪人
- 经纪团队行政助理
- 房源营销协调员

### 4.2 次要用户

- 物业管理人员
- 独立房东
- 房地产摄影与营销服务商

## 5. 主要使用场景

### 场景 A：交互式创建房源

经纪人运行填写脚本，根据提示输入房源、照片和联系方式，程序保存 YAML 并生成 PNG/PDF。

### 场景 B：批量复用模板

行政助理把多套 YAML/JSON 放入同一文件夹，程序先验证全部资料，再通过一次命令生成多套风格一致的完整海报、社交媒体图片和批次摘要。

### 场景 C：品牌主题切换

不同经纪团队修改主题颜色、标志、字体、品牌宣传语和网站，继续使用相同排版引擎。

### 场景 D：发布前验证

用户先运行仅验证模式，检查必填字段、电话、邮箱、图片路径和列表长度，通过后再生成正式海报。

### 场景 E：主图焦点选择

用户生成离线 HTML 页面，在完整主图上点击重点位置，查看横幅裁切结果并复制 `hero_focal` YAML。

### 场景 F：视觉回归审核

开发者或营销负责人将候选海报与已批准基准图比较，读取差异指标并查看可选差异图，确认变化是预期结果后再发布。

### 场景 G：浏览器本地制作整套广告

经纪人打开在线或本地托管的可视化编辑器，在一个页面中填写完整房源、经纪人、品牌与中英文信息，整理主图、室内照片、户型图和双标志，选择合规配置与主图重点位置，并实时预览完整海报与四种社交版式。所有资料只保存在当前设备的浏览器本地存储中。

### 场景 H：生成后发现错误并继续修改

经纪人生成或导出海报后发现错误，返回编辑器，在需要时恢复最新本地草稿，只修改有误字段，然后重新生成；不必重新填写无关资料，也不必重新选择仍可恢复的图片。

### 场景 I：通过一个授权 MLS 号码生成

获授权经纪人选择另行运营的连接器，输入一个 MLS 号码，取得唯一精确匹配的房源，核对映射字段和允许使用的图片，修正本地例外，并且只有在 MLS、申请要求、合规和图片许可门禁全部通过后才能导出。`DEMO1234` 提供完全虚构的离线体验路径。

## 6. 用户流程

1. 安装 Python 依赖。
2. 选择交互式填写或编辑 YAML/JSON 模板。
3. 填写房源和联系方式。
4. 指定主图、室内照片、户型图和品牌标志。
5. 运行验证。
6. 修正程序报告的输入错误。
7. 需要时生成焦点预览并更新主图焦点。
8. 生成完整 PNG、可选 PDF、社交尺寸和清单文件。
9. 审核发现错误时，返回已保存的编辑状态，修正后重新生成。
10. 对重要模板运行视觉回归比较。
11. 经纪人进行事实、合规和视觉审核。
12. 发布或打印最终海报。

在 v2 候选流程中，第 2–4 步可以从明确触发的 MLS 号码生成开始。替换现有资料前必须保存恢复快照，刷新时必须显示差异，完成后必须保留完整导入项目。

浏览器编辑器提供一条独立的免安装完整流程，可填写所有核心资料，管理主图、室内照片、户型图与双标志，设置焦点，运行与 Python 规则对齐的验证，使用合规配置与品牌模板，预览三种语言模式的五种版式，并导出 PNG、浏览器打印 PDF、项目 JSON/YAML、社交 ZIP、清单或审批包。文件夹批处理和像素级视觉回归继续由 Python 命令行工具提供。

## 7. 功能需求

### 7.1 结构化输入

系统必须支持 YAML 和 JSON，并包含以下字段组：

| 字段组 | 必须支持的内容 |
|---|---|
| 房源身份 | 状态、地址、单元号、城市、邮编、宣传语 |
| 价格信息 | 租金或售价、计价周期、MLS 编号 |
| 房屋数据 | 卧室、卫生间、面积、楼层、朝向、车位、可入住日期 |
| 详细内容 | 租赁条件、房屋特色、大楼设施、水电费用、周边亮点 |
| 经纪人 | 姓名、职位、电话、邮箱 |
| 品牌 | 公司或团队名称、宣传语、网站、标志 |
| 图片 | 主图、室内照片、户型图、主图焦点 |
| 主题 | 背景色、纸张色、文字色、强调色、字体 |
| 画布 | 宽度、高度、DPI |

### 7.2 数据验证

系统必须：

- 检查所有必填字段
- 验证电子邮箱基本格式
- 验证电话号码包含 10 至 15 位数字
- 验证卧室、卫生间和面积为有效正数
- 支持 `600-699` 形式的面积范围
- 验证主题颜色为六位十六进制颜色
- 验证照片、户型图、标志和自定义字体文件存在
- 验证主图焦点位于 `0` 至 `1` 范围
- 限制各信息列表长度，防止版面溢出
- 在发现错误时停止渲染并给出可理解的错误信息

### 7.3 照片处理

系统必须：

- 读取并应用 EXIF 方向
- 使用高质量重采样缩放图片
- 保持图片比例，不得拉伸
- 按目标区域自动裁切
- 支持主图焦点控制
- 支持透明背景品牌标志
- 支持最多四张室内照片输入
- 在海报中展示主图、至少一张室内照片和可选户型图

### 7.4 文字排版

系统必须：

- 使用确定性字体渲染所有文字
- 根据可用宽度自动缩小单行文字
- 根据可用宽高自动换行
- 对过长文本提供安全截断机制
- 保证地址、价格、MLS、电话和邮箱保持输入内容
- 在常见内容长度下避免重叠和越界

### 7.5 视觉版式

海报必须包含：

- 租售状态标签
- 地址和单元号
- 价格与计价周期
- MLS 编号
- 卧室、卫生间、面积、楼层、朝向和车位数据栏
- 可入住日期
- 房源照片和户型图区域
- 租赁条件、特色、设施和水电费用信息卡
- 周边亮点栏
- 经纪人、公司、电话、邮箱、网站和品牌标志页脚

整体设计应当参考用户提供样图的信息架构，但不得逐像素复制其排版或装饰。

### 7.6 品牌配置

系统必须支持：

- 七种核心颜色配置
- 常规、粗体和衬线字体路径
- 品牌标志
- 公司或团队名称
- 品牌宣传语和网站
- 在浅色和深色背景上保持标志可读

### 7.7 输出

系统必须：

- 输出 RGB PNG
- 可选输出单页 PDF
- 输出包含输入、资源和成品 SHA-256 的清单 JSON
- 可选输出 1080 × 1080、1080 × 1350、1080 × 1920 和 1200 × 630 社交媒体 PNG
- 批量模式输出包含每套房源结果的 `batch-summary.json`
- 自动创建输出目录
- 使用稳定且可预测的文件名
- 在命令结束时显示所有输出路径

### 7.8 使用入口

系统必须提供：

1. `generate_poster.py`：从现有 YAML/JSON 生成海报。
2. `scripts/new_listing.py`：通过交互式问题创建 YAML 并可立即渲染。
3. `python -m realtor_poster`：标准 Python 模块入口。
4. `realtor-poster`：安装后的命令行入口。
5. `scripts/focal_preview.py` 与 `realtor-poster-preview`：生成离线焦点预览。
6. `scripts/visual_regression.py` 与 `realtor-poster-visual`：执行视觉回归比较。

### 7.9 批量处理

系统必须：

- 接受包含多套 YAML、YML 或 JSON 的文件夹
- 递归发现房源输入并按稳定文件名顺序处理
- 排除隐藏文件、生成清单和批次摘要
- 在渲染前验证全部输入；任何一套无效时不产生部分批次
- 对重复文件名生成无冲突输出名
- 为每套房源保留独立清单，并为整个批次生成摘要

### 7.10 社交媒体版式

系统必须：

- 支持 `square`、`portrait`、`story` 和 `landscape` 四种预设
- 支持通过 `all` 一次选择全部预设
- 使用主图焦点进行各尺寸自适应裁切，不得拉伸照片
- 保留状态、地址、单元号、价格、MLS、核心房屋数据、经纪人和联系方式
- 使用与完整海报相同的品牌颜色、字体和标志
- 把社交图片哈希纳入房源清单

### 7.11 离线焦点预览

系统必须：

- 生成无需网络连接和外部资源的单一 HTML 文件
- 嵌入完整主图、横幅裁切预览和当前海报缩略图
- 允许用户点击完整主图选择归一化焦点坐标
- 即时更新裁切位置和三位小数 YAML
- 提供复制操作，同时保留手动复制后备路径
- 不向外部网站发送或上传房源图片

### 7.12 视觉回归

系统必须：

- 验证基准图与候选图尺寸一致
- 计算 0 至 1 的标准化平均绝对差异
- 计算明显变化的像素比例和最大通道差异
- 允许调用者设置通过阈值
- 可选保存增强后的差异图
- 通过进程返回码区分通过、超过阈值和输入错误

### 7.13 浏览器本地可视化编辑器

系统必须：

- 不要求账号、服务器上传或分析代码
- 在当前设备的浏览器本地存储中处理照片、标志、联系方式和项目文件
- 支持拖放或选择主图与透明背景标志
- 允许用户点击主图并调整水平、垂直焦点
- 实时预览完整海报、方形、竖版、限时动态和横版
- 支持下载 PNG、通过浏览器打印为 PDF、导出四种社交图片 ZIP
- 支持保存和重新打开包含表单、主题、焦点及图片的项目 JSON
- 支持项目 JSON/YAML 往返、室内照片排序、更换与移除、户型图和明暗标志
- 支持租赁、出售、开放日和刚刚上市合规配置，可按配置要求经纪人职衔与执照/注册资料，并在错误未清除时阻止导出
- 支持携带颜色、字体、双标志及默认版式的版本化品牌模板、选择性字段锁定、复制、重命名和模板导入导出
- 支持英文、中文和双语文字及五种对应版式；双语标题与特色分别测量、换行并形成清晰层级
- 支持与已批准项目比较、审核人/日期/备注记录和完整审批 ZIP
- 生成记录语言、配置、模板、资产与输出 SHA-256 的本地清单
- 保存 3–8 项排序房屋数据、双户型图、最多三项重点卖点、结构化租约详情和排序租金包含项目
- 保存排序租客承担费用；同一费用同时标记为租金包含时必须阻止导出
- 保存最多十二项双语设施和最多十项双语申请要求，并使用可复用本地图标
- 存在申请要求时，导出前必须明确确认并显示信息性免责声明
- 支持照片、插画、姓名首字母和无头像四种经纪人资料模式，并提供头像焦点、双语标语、双语行动号召和联系方式
- 使用原创模块化打印版式与按优先级精简的社交版式，不复制用户提供的参考图
- 在短暂防抖后，以及导出、重置或替换项目前，把包含本地图片的完整项目结构自动保存到 IndexedDB
- 再次打开编辑器时显示最新兼容草稿的项目名与保存时间，并在用户选择恢复后还原控件、本地媒体和滚动位置
- 按项目标识隔离草稿，检测另一标签页保存的同项目较新草稿，并在用户处理冲突前暂停自动保存
- 对重置、导入和清除操作要求明确确认；存储配额、受限浏览模式、损坏或版本不兼容时必须显示警告，并允许在删除前下载恢复数据
- 恢复数据仅保存在设备上，不上传、不分析、不要求账号、不承诺云同步或跨设备恢复；同时保留便携项目下载与本地草稿清除入口
- 能够从 GitHub Pages 在线打开，也能通过 `scripts/serve_web.py` 在本机运行

### 7.14 双语文档

系统必须提供：

- `README.md` 中的完整中文使用说明
- `README.en.md` 中的完整英文使用说明
- `PRD.md` 中的完整中文产品需求文档
- `PRD.en.md` 中的完整英文产品需求文档
- 每份文档开头直接可用的语言切换链接
- 两种语言一致的当前版本与已实现功能范围

### 7.15 授权 MLS 号码生成候选功能

- 用户明确选择授权连接器并点击“生成”前，浏览器不得发出 MLS 请求。
- 生产连接器必须使用 HTTPS；只有本机开发可使用回环 HTTP。地址不得包含凭据、查询令牌或片段。
- 连接器必须只返回一个已确认的数据商/MLS 系统/号码/状态/地址/单元号精确匹配，并且房源状态必须为受支持的有效状态。
- 映射必须确定性执行，不得猜测、翻译、摘要或改写房源陈述。
- 租约详情、租金包含、租客承担费用、设施、申请要求、允许使用的图片及双户型图必须进入现有 v1.4 结构，同时保留经纪人、品牌、主题、模板和合规资料。
- 每个导入字段和媒体记录必须保存数据商、房源、获取时间、来源编号、许可及本地修改信息。没有明确导出许可的媒体不得进入成品。
- 刷新必须先显示差异，并在替换导入值或本地修改前要求确认。
- 在用户核对房源事实、当前状态、披露、申请要求和图片使用权之前，导入项目不得发布导出；修改导入字段或图片会使审核失效。
- 浏览器不得接收 MLS 密码、API Key、Bearer Token 或真实数据商示例；认证及数据商凭据只属于另行运营的连接器。

## 8. 非功能需求

### 8.1 兼容性

- 支持 Python 3.9 或更高版本
- 支持 macOS 和常见 Linux 环境
- 主要依赖限制为 Pillow 和 PyYAML
- 浏览器可视化编辑器支持当前主流浏览器

### 8.2 性能

- 1800 × 2400 示例海报应在普通个人电脑上数秒内完成
- 四种社交媒体样例应能在同一命令中完成
- 图片处理不得无限制占用内存
- 字体查找和加载应使用缓存
- 普通字段修改后，浏览器编辑器应保持交互式实时更新

### 8.3 可维护性

- 配置验证、绘图辅助、完整海报、社交版式、批量处理、预览、视觉比较和命令行入口应分离
- 核心函数应有清晰的说明和注释
- 新主题不应要求修改业务数据结构
- 示例数据不得包含真实个人联系方式
- 中文与英文文档应在同一版本中一起更新

### 8.4 可追溯性

- 清单文件必须记录生成器版本
- 必须记录输入文件哈希
- 必须记录所有使用资源的哈希
- 必须记录 PNG/PDF 输出哈希
- 必须记录所有社交媒体 PNG 输出哈希

### 8.5 隐私

- 手工编辑和虚构演示不得上传房源资料或图片
- 只有用户明确点击“生成”后，授权连接器才可接收规范化 MLS 号码；不得后台查询
- 浏览器编辑器不得包含分析或跟踪代码
- 可移植项目文件只能通过用户明确操作创建
- 清单文件不得包含不必要的绝对本地路径或个人资料

## 9. 验收标准

v1.4.0 正式版必须满足：

1. 示例 YAML 可以通过验证。
2. 示例可以生成 1800 × 2400 RGB PNG。
3. 示例可以生成一页完整 PDF。
4. 地址、价格、MLS、电话和邮箱没有拼写变化。
5. 主图、室内照片、户型图和品牌标志均能正确显示。
6. 六项核心房屋数据在信息栏中清晰可读。
7. 四类详细信息卡没有文字重叠。
8. 非法邮箱和电话号码会被拒绝。
9. 缺失图片会在渲染前报告。
10. `600-699` 面积范围能够通过验证并显示。
11. 十二项自动化测试全部通过。
12. README 提供从安装到生成成品的完整中文与英文命令。
13. 文件夹输入能够在一次命令中生成多套房源，并产生批次摘要。
14. 批量模式遇到无效房源时不会留下部分生成结果。
15. 四种社交版式均输出正确尺寸的 RGB PNG。
16. 社交版式保留地址、价格、MLS 和联系方式。
17. 焦点预览 HTML 不引用外部图片或脚本资源。
18. 同一环境内连续渲染相同输入产生像素一致的结果。
19. 视觉回归工具能够通过完全一致图片并拒绝明显变化图片。
20. 清单记录生成器版本 1.4.0 和社交媒体文件 SHA-256。
21. 浏览器编辑器不包含上传或分析代码，图片与表单资料只在当前标签页处理。
22. 浏览器编辑器能够预览并导出完整海报与四种社交媒体尺寸。
23. 浏览器项目 JSON 可以保存并重新打开表单、主题、焦点和已选图片。
24. 中文与英文 README、PRD 均包含清晰的语言切换链接和同等功能范围。
25. 浏览器项目 JSON/YAML 能保留全部房源字段、室内照片顺序、户型图和双标志。
26. 合规配置可要求经纪公司、经纪人职衔、执照/注册资料和联系方式；必填错误会阻止所有发布导出，并明确声明工具不提供法律批准。
27. 版本化品牌模板可携带颜色、字体、双标志及默认版式，支持复制/重命名和选定字段锁定，且项目级未锁定字段仍可修改。
28. 英文、中文和双语内容均可生成五种版式；中文使用合适的系统字体回退，双语标题与特色分别测量和换行。
29. 审批状态只有在审核人和日期完整时才能设为已批准；审批包含五张校样、源数据、审核记录、清单和 SHA-256 目录。
30. 房屋数据栏支持卧室、卫浴、面积、楼层、朝向、阳台、车位和自定义项目；打印版显示最多八项，社交版显示四项优先数据，隐藏后不得留下空格。
31. 三维家具户型图和二维技术户型图可独立添加、替换、删除、排序及设置等比适应、按宽度适应或裁切；低分辨率会产生警告，原图像素尺寸和 SHA-256 会进入清单。
32. 最多三项重点卖点可保存本地图片、中英文标题/详情、圆形/圆角方形/矩形遮罩和独立焦点；空项目不得占据海报空间。
33. 租约详情最多显示九行，可从租期、入住日期、押金、付款、保险、钥匙、宠物、吸烟、停车和自定义项目中选择，并允许排序、隐藏或标记不适用；出售项目自动收起该模块。
34. 租金包含项目支持标准与自定义费用、排序、删除和待确认状态；所有内容进入项目、差异比较、清单和审批包。
35. 租客承担费用支持标准与自定义类别、排序、显示状态和双语标签；与启用的租金包含项目重复时必须作为阻止导出的错误，出售项目自动收起两类费用模块。
36. 最多十二项双语设施可添加、排序、隐藏并使用可复用本地图标；打印模块保留可见顺序，无项目时自动收起且不留下空格。
37. 最多十项申请要求可添加、排序或隐藏；存在启用要求时，导出前必须由用户明确确认并显示信息性免责声明；出售项目自动收起该模块。
38. 经纪人页脚支持照片、插画、姓名首字母和无头像四种模式，缺图时安全回退，并支持头像焦点、双语标语/行动号召、职衔、经纪公司、电话、邮箱、网站和标志。
39. 1800 × 2400 打印海报把故事 1–9 组合为一套原创模块化信息层级且不重叠；方形、竖版、限时动态和横版保留房源核心身份与紧凑优先摘要。
40. 生成 PNG/PDF/社交 ZIP/清单/审批 ZIP 不得清空可编辑项目，并且每次导出必须先发起本地恢复快照保存。
41. 再次打开编辑器时应显示最新兼容 IndexedDB 草稿，并能恢复全部项目字段、嵌入的主图/室内图/户型图/标志/卖点图/头像、项目标识和滚动位置。
42. 重置及项目/模板/合规配置导入前必须确认并保存操作前快照；用户还可下载便携项目并明确清除本地草稿。
43. 存储配额不足、受限浏览模式、恢复记录损坏或不兼容、跨标签页冲突时，必须显示双语警告，不得静默丢失数据。
44. 自动化恢复测试覆盖包含图片的完整快照、项目隔离、最新记录选择、结构版本兼容及生成/导出/重置恢复挂钩。
45. `DEMO1234` 在不联网、不使用真实房源的情况下生成完整虚构 v1.4 项目。
46. 授权连接器只接收一个规范化 MLS 号码，并拒绝不安全或带令牌的地址、非唯一/非精确匹配、不支持或撤销的状态、过大/格式错误的响应；数据源错误提供双语提示。
47. 房源事实、全部支持的结构化模块、允许使用的图片与户型图确定性映射，同时完整保留经纪人、品牌、主题、模板和合规资料。
48. 没有明确导出许可的媒体必须排除；项目与清单保存来源编号、尺寸、顺序、许可依据、确认时间及本地媒体修改。
49. 项目 JSON/YAML 往返必须保留 MLS 来源、逐字段来源、获取/导入时间、刷新次数、禁用媒体记录及用户覆盖。
50. 导入与刷新沿用 Issue #20 快照；刷新先显示差异并在替换前要求确认，失败时保留当前编辑状态。
51. 只有申请要求确认及 MLS 事实/状态/披露/图片权利人工审核都通过后才允许发布导出；修改导入字段或图片会使 MLS 审核失效。

## 10. 风险与应对

| 风险 | 应对措施 |
|---|---|
| 用户填写错误房源事实 | 强制验证，并要求发布前人工审核 |
| 图片比例差异过大 | 自动等比裁切并提供主图焦点 |
| 文字过多导致溢出 | 自动缩放、换行、列表数量限制 |
| 不同系统字体造成差异 | 支持明确指定字体文件，并记录资源哈希 |
| 品牌标志在深色背景不可读 | 使用对比底板保护标志 |
| PDF 与 PNG 视觉不一致 | PDF 直接使用同一确定性画布导出并进行页面渲染检查 |
| 广告不符合监管要求 | README 明确要求经纪人和经纪公司最终审核 |
| 示例内容被误认为真实房源 | 在示例中显示“示例房源”并使用虚构资料 |
| 敏感房源资料被意外上传 | 浏览器本地处理，不设置上传端点或分析代码 |
| 生成、刷新或纠错后编辑输入丢失 | 防抖 IndexedDB 自动保存、操作前快照、双语恢复界面和便携项目下载 |
| 另一浏览器标签页覆盖较新工作 | 项目标识隔离、BroadcastChannel 冲突检测，并在用户处理前暂停保存 |
| 浏览器存储不可用或空间已满 | 明确双语警告并提示下载便携项目；保存失败时绝不显示成功 |
| 中英文文档逐渐不一致 | 同一版本更新、直接语言链接与范围检查 |
| 未授权或过期的房源资料 | 明确选择数据商、精确匹配协议、有效状态验证、获取时间、过期提示和强制人工核对 |
| 数据商图片没有广告许可 | 默认拒绝、记录禁用媒体、明确许可审核，并在清单保存来源元数据 |
| 静态托管暴露连接器凭据 | 浏览器不提供令牌输入；安全会话认证与所有数据商凭据都保留在另行运营的连接器 |

## 11. 后续版本规划

### 早期候选需求

- 增加二维码支持

### 1.2 已完成

- 支持批量读取多套 YAML、YML 或 JSON，并在全量验证后统一输出
- 自动生成四种自适应社交媒体尺寸版本
- 增加无需联网的版面预览和主图焦点选择界面
- 增加像素确定性、社交尺寸、批量处理和视觉差异自动化检查
- 增加浏览器本地可视化编辑器、五种实时预览、便携项目与 ZIP 导出
- 提供完整的中文与英文 README、PRD 文档

### 1.3 已完成

- 完整浏览器本地广告编辑器及版本化项目结构
- 合规配置与阻止导出的预检门禁
- 可移植品牌模板与选择性字段锁定
- 英文、中文和双语五尺寸广告
- 基准比较、审核记录与带校验值的审批包

### 1.4 已完成（故事 1–10）

- 完整可排序房屋数据栏及社交尺寸优先级
- 三维家具户型图与二维技术户型图并列展示
- 最多三项本地图像重点卖点
- 结构化双语租约详情
- 带本地 MIT 图标的租金包含项目
- 租客承担费用，以及阻止导出的包含/承担重复检查
- 使用可复用图标、无内容时自动收起的排序设施列表
- 带确认与免责声明门禁的申请要求清单
- 四种头像模式的经纪人资料与双语行动号召页脚
- 整合十个故事的原创模块化打印版式和自适应社交版式
- 横向 Issue #20：为全部 v1.4 模块提供由项目结构驱动的设备内自动保存与恢复，不改变故事 1–10 编号

### 2.0

- Issue #22 授权 MLS 号码导入基础：虚构离线示例、精确匹配协议、来源记录、许可过滤、恢复、刷新差异和审核门禁
- 可选桌面图形界面
- 经纪团队共享配置
- 在获得授权后连接外部房源数据源

## 12. 合规原则

- 不自动生成、猜测或夸大房源事实
- 不把示例资料当成真实广告发布
- 不存储账号密码或访问令牌
- 不在输出清单中保存不必要的个人隐私数据
- 所有最终广告必须由负责的经纪人或经纪公司审核

Copyright © 2026 **Daniel Xu**。本文档与项目均根据 [MIT License](LICENSE) 开源。
