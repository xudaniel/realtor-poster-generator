# 房地产海报生成器产品需求文档

## 1. 文档信息

- 产品名称：房地产海报生成器
- 当前版本：1.0
- 文档语言：中文
- 产品形态：本地 Python 命令行工具
- 主要输出：PNG、PDF、校验清单 JSON

## 2. 产品背景

房地产经纪人在制作租赁或销售海报时，通常需要重复整理地址、价格、MLS、房屋数据、特色、设施、照片、户型图和联系方式。使用普通设计软件容易产生以下问题：

- 每套房源都要重新排版，耗时较长
- 地址、电话号码、价格或 MLS 可能被手动输错
- 照片比例不一致，容易被拉伸或错误裁切
- 文字过长时可能溢出、遮挡或变得难以阅读
- 不同经纪人制作的海报缺乏统一品牌风格
- 输出文件缺少可追溯的输入和资源校验信息

本产品通过结构化输入、确定性文字渲染、自动排版和统一品牌配置，将房源资料快速转换为专业的竖版海报。

## 3. 产品目标

### 3.1 核心目标

1. 经纪人只填写一次结构化数据，即可生成完整海报。
2. 所有关键文字必须严格来自输入文件，不允许模型自动改写或猜测。
3. 常见字段错误和资源缺失必须在渲染前被发现。
4. 不同尺寸的照片必须自动旋转、缩放和裁切，不得变形。
5. 海报应具有稳定、专业、易读且可配置的品牌视觉效果。
6. 同一输入、图片、字体和软件版本应当产生可重复的视觉结果。
7. 输出应同时满足线上分享、打印和归档需要。

### 3.2 非目标

- 不直接连接 MLS 系统抓取房源资料
- 不自动发布到社交媒体或房地产平台
- 不判断广告内容是否合法或事实是否准确
- 不生成或修改真实房源照片
- 不替代专业设计师处理高度定制的广告活动
- 不提供多人在线协作或云端账户系统

## 4. 目标用户

### 4.1 主要用户

- 房地产销售代表
- 房地产经纪人
- 经纪团队行政助理
- 房源营销协调员

### 4.2 次要用户

- 物业管理人员
- 独立房东
- 房地产摄影与营销服务商

## 5. 主要使用场景

### 场景 A：交互式创建房源

经纪人运行填写脚本，根据提示输入房源、照片和联系方式，程序保存 YAML 并生成 PNG/PDF。

### 场景 B：批量复用模板

行政助理复制 YAML 模板，替换房源信息和照片路径，通过相同命令生成多套风格一致的海报。

### 场景 C：品牌主题切换

不同经纪团队修改主题颜色、标志、字体、品牌宣传语和网站，继续使用相同排版引擎。

### 场景 D：发布前验证

用户先运行仅验证模式，检查必填字段、电话、邮箱、图片路径和列表长度，通过后再生成正式海报。

## 6. 用户流程

1. 安装 Python 依赖。
2. 选择交互式填写或编辑 YAML/JSON 模板。
3. 填写房源和联系方式。
4. 指定主图、室内照片、户型图和品牌标志。
5. 运行验证。
6. 修正程序报告的输入错误。
7. 生成 PNG、PDF 和清单文件。
8. 经纪人进行事实、合规和视觉审核。
9. 发布或打印最终海报。

## 7. 功能需求

### 7.1 结构化输入

系统必须支持 YAML 和 JSON，并包含以下字段组：

| 字段组 | 必须支持的内容 |
|---|---|
| 房源身份 | 状态、地址、单元号、城市、邮编、宣传语 |
| 价格信息 | 租金或售价、计价周期、MLS 编号 |
| 房屋数据 | 卧室、卫生间、面积、楼层、朝向、车位、可入住日期 |
| 详细内容 | 租赁条件、房屋特色、大楼设施、水电费用、周边亮点 |
| 经纪人 | 姓名、职位、电话、邮箱 |
| 品牌 | 公司或团队名称、宣传语、网站、标志 |
| 图片 | 主图、室内照片、户型图、主图焦点 |
| 主题 | 背景色、纸张色、文字色、强调色、字体 |
| 画布 | 宽度、高度、DPI |

### 7.2 数据验证

系统必须：

- 检查所有必填字段
- 验证电子邮箱基本格式
- 验证电话号码包含 10 至 15 位数字
- 验证卧室、卫生间和面积为有效正数
- 支持 `600-699` 形式的面积范围
- 验证主题颜色为六位十六进制颜色
- 验证照片、户型图、标志和自定义字体文件存在
- 验证主图焦点位于 `0` 至 `1` 范围
- 限制各信息列表长度，防止版面溢出
- 在发现错误时停止渲染并给出可理解的错误信息

### 7.3 照片处理

系统必须：

- 读取并应用 EXIF 方向
- 使用高质量重采样缩放图片
- 保持图片比例，不得拉伸
- 按目标区域自动裁切
- 支持主图焦点控制
- 支持透明背景品牌标志
- 支持最多四张室内照片输入
- 在海报中展示主图、至少一张室内照片和可选户型图

### 7.4 文字排版

系统必须：

- 使用确定性字体渲染所有文字
- 根据可用宽度自动缩小单行文字
- 根据可用宽高自动换行
- 对过长文本提供安全截断机制
- 保证地址、价格、MLS、电话和邮箱保持输入内容
- 在常见内容长度下避免重叠和越界

### 7.5 视觉版式

海报必须包含：

- 租售状态标签
- 地址和单元号
- 价格与计价周期
- MLS 编号
- 卧室、卫生间、面积、楼层、朝向和车位数据栏
- 可入住日期
- 房源照片和户型图区域
- 租赁条件、特色、设施和水电费用信息卡
- 周边亮点栏
- 经纪人、公司、电话、邮箱、网站和品牌标志页脚

整体设计应当参考用户提供样图的信息架构，但不得逐像素复制其排版或装饰。

### 7.6 品牌配置

系统必须支持：

- 七种核心颜色配置
- 常规、粗体和衬线字体路径
- 品牌标志
- 公司或团队名称
- 品牌宣传语和网站
- 在浅色和深色背景上保持标志可读

### 7.7 输出

系统必须：

- 输出 RGB PNG
- 可选输出单页 PDF
- 输出包含输入、资源和成品 SHA-256 的清单 JSON
- 自动创建输出目录
- 使用稳定且可预测的文件名
- 在命令结束时显示所有输出路径

### 7.8 使用入口

系统必须提供：

1. `generate_poster.py`：从现有 YAML/JSON 生成海报。
2. `scripts/new_listing.py`：通过交互式问题创建 YAML 并可立即渲染。
3. `python -m realtor_poster`：标准 Python 模块入口。
4. `realtor-poster`：安装后的命令行入口。

## 8. 非功能需求

### 8.1 兼容性

- 支持 Python 3.9 或更高版本
- 支持 macOS 和常见 Linux 环境
- 主要依赖限制为 Pillow 和 PyYAML

### 8.2 性能

- 1800 × 2400 示例海报应在普通个人电脑上数秒内完成
- 图片处理不得无限制占用内存
- 字体查找和加载应使用缓存

### 8.3 可维护性

- 配置验证、绘图辅助、版面渲染和命令行入口应分离
- 核心函数应有清晰的说明和注释
- 新主题不应要求修改业务数据结构
- 示例数据不得包含真实个人联系方式

### 8.4 可追溯性

- 清单文件必须记录生成器版本
- 必须记录输入文件哈希
- 必须记录所有使用资源的哈希
- 必须记录 PNG/PDF 输出哈希

## 9. 验收标准

产品达到 1.0 验收要求时，必须满足：

1. 示例 YAML 可以通过验证。
2. 示例可以生成 1800 × 2400 RGB PNG。
3. 示例可以生成一页完整 PDF。
4. 地址、价格、MLS、电话和邮箱没有拼写变化。
5. 主图、室内照片、户型图和品牌标志均能正确显示。
6. 六项核心房屋数据在信息栏中清晰可读。
7. 四类详细信息卡没有文字重叠。
8. 非法邮箱和电话号码会被拒绝。
9. 缺失图片会在渲染前报告。
10. `600-699` 面积范围能够通过验证并显示。
11. 三项自动化测试全部通过。
12. README 提供从安装到生成成品的完整中文命令。

## 10. 风险与应对

| 风险 | 应对措施 |
|---|---|
| 用户填写错误房源事实 | 强制验证，并要求发布前人工审核 |
| 图片比例差异过大 | 自动等比裁切并提供主图焦点 |
| 文字过多导致溢出 | 自动缩放、换行、列表数量限制 |
| 不同系统字体造成差异 | 支持明确指定字体文件，并记录资源哈希 |
| 品牌标志在深色背景不可读 | 使用对比底板保护标志 |
| PDF 与 PNG 视觉不一致 | PDF 直接使用同一确定性画布导出并进行页面渲染检查 |
| 广告不符合监管要求 | README 明确要求经纪人和经纪公司最终审核 |
| 示例内容被误认为真实房源 | 在示例中显示“示例房源”并使用虚构资料 |

## 11. 后续版本规划

### 1.1

- 增加“出售”专用价格和开放日版式
- 提供更多可选主题
- 增加中英文双语字段
- 增加二维码支持

### 1.2

- 支持批量读取多套 YAML
- 自动生成社交媒体尺寸版本
- 增加版面预览和焦点选择界面
- 增加更多自动化视觉回归检查

### 2.0

- 可选桌面图形界面
- 品牌模板管理
- 经纪团队共享配置
- 在获得授权后连接外部房源数据源

## 12. 合规原则

- 不自动生成、猜测或夸大房源事实
- 不把示例资料当成真实广告发布
- 不存储账号密码或访问令牌
- 不在输出清单中保存不必要的个人隐私数据
- 所有最终广告必须由负责的经纪人或经纪公司审核
